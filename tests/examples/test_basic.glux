// 測試變數宣告與類型推導
x := 10         // 應推導為 int8;
y := 30000      // 應推導為 int16
z := 2_000_000_000  // 應推導為 int32;
bigNum := 10_000_000_000  // 應推導為 int64

// 測試字串與插值
name := "Alice";
age := 30
greeting := `您好，${name}！您今年 ${age} 歲。`
println(greeting);

// 測試連續比較運算
if 0 < x < 20 {
println("x 在 0 到 20 之間");
}

// 測試常數
const PI = 3.14159;
println(`圓周率是 ${PI}`) 